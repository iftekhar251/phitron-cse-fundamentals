#include <bits/stdc++.h>
using namespace std;
int main()
{
    // vector<int> v;  type 1
    // vector<int> v(10);  type 2
    // vector<int> v(10,-7);  //type 3
    // vector<int> v2(v);    type 4
    // int a[4]={1,4,4,5};
    // vector<int> v(a,a+4);  type 5
    vector<int> v = {4, 5, 70};
    for (int i = 0; i < v.size(); i++)
        cout << v[i] << endl;
    return 0;
}